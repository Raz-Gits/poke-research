.venv/
__pycache__/
*.pyc
.DS_Store
data/raw/
# raw TCGCSV daily archives (~380MB, re-downloadable); keep the derived
# data/history/ panel (small) so the backtest reruns without re-fetching.
data/tcgcsv_archive/
# secrets — never commit your eBay keys
.env
# Google Trends scratch (collector built + tested, NOT wired into the model)
data/trends.json
data/_trends_fetch.log
# formula-eval scratch runners (keep the reusable harness experiments/eval_formula.py)
experiments/_run_*.py
experiments/_paired_diff_check.py
.env.*
!.env.example
!*.env.sops
!.env.*.sops
node_modules/
*.bak-devsync
